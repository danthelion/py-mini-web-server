A minimal web server implementation in Python capable of serving `html` pages, listing directories and running CGI scripts.

`$ python mini-web-server`
`$ curl localhost:8080`
`$ curl localhost:8080/test`
`$ curl localhost:8080/test/test.py`